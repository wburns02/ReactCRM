# API Configuration
# The backend API URL

# For new FastAPI backend (React CRM API v2)
VITE_API_URL=http://localhost:5001/api/v2

# For legacy Flask backend (if still using shared backend)
# VITE_API_URL=http://localhost:5000

# For production deployment
# VITE_API_URL=https://react-api.yourdomain.com/api/v2

# Google Analytics 4 (Optional)
# Set your GA4 Measurement ID to enable tracking
# Get this from: Google Analytics > Admin > Data Streams > Web > Measurement ID
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
