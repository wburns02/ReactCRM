/**
 * SEO Head Component
 *
 * JSON-LD structured data (LocalBusiness + FAQPage) is now in static index.html
 * so crawlers can see it without JavaScript execution.
 *
 * This component is kept for any future dynamic SEO needs but currently
 * renders nothing since the structured data is static.
 */
export function SEOHead() {
  // Structured data is now in index.html for crawler visibility
  return null;
}
