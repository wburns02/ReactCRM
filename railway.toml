[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/"
healthcheckTimeout = 60
numReplicas = 1

# Explicitly set the port Railway should use
[deploy.healthcheck]
path = "/"
timeout = 60
